markdown

# Blueprint Cleanup Script

Deno script for cleaning up old blueprints.

## Prerequisites

- Install deno

```bash
brew install deno
```

- Environment variables set in `.env` file

## Usage

In cleanup directory, run the script:

```bash
deno task cleanup
```
